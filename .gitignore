build
dist / media
node_modules
.DS_Store
# Semua varian .env (.env.local, .env.production, ...) — repo ini publik,
# jadi jangan andalkan pola tunggal. Pengecualian: contoh tanpa nilai asli.
.env
.env.*
!.env.example
!.env.test.example
.next
next-env.d.ts
.vercel

# Artefak TypeScript incremental build
tsconfig.tsbuildinfo

# SQLite database (lokal, jangan di-commit)
*.db
*.db-journal
*.db-wal

# Payload default media upload directory
public/media/

public/robots.txt
public/sitemap*.xml


# Hasil audit keamanan — berkas kerja lokal. Isinya memetakan permukaan
# serang beserta lokasi barisnya, jadi jangan diterbitkan di repo publik.
.audit/
AUDIT_REPORT.md

# Playwright
node_modules/
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
